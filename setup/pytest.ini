[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --tb=short -q
markers =
    live: marks tests that require GEMINI_LIVE_TESTS=1 (deselect with '-m "not live"')
    record: marks tests that record live API responses (deselect with '-m "not record"')
